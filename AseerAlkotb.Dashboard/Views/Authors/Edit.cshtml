@model AseerAlkotb.Application.Features.Authors.Responses.UpdateAuthorResponse;
@{
    ViewData["Title"] = "Edit Author";
}

<div class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1 class="m-0">Edit Author</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="/">Dashboard</a></li>
                    <li class="breadcrumb-item"><a asp-action="Index">Authors</a></li>
                    <li class="breadcrumb-item active">Edit</li>
                </ol>
            </div>
        </div>
    </div>
</div>

<section class="content">
    <div class="container-fluid">
        <div class="card">
            <div class="card-body">
                <form asp-action="Edit" enctype="multipart/form-data" id="editAuthorForm">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                    <div class="row">
                        <!-- LEFT: Author Info -->
                        <div class="col-md-8">
                            <div class="card mb-4">
                                <div class="card-header">
                                    <h3 class="card-title">Author Information</h3>
                                </div>
                                <div class="card-body">
                                    <div class="form-group">
                                        <label asp-for="Id"></label>
                                        <input asp-for="Id" class="form-control" readonly />
                                    </div>

                                    <div class="form-group">
                                        <label asp-for="Name"></label>
                                        <input asp-for="Name" class="form-control" placeholder="Enter author name" />
                                        <span asp-validation-for="Name" class="text-danger"></span>
                                    </div>

                                    <div class="form-group">
                                        <label asp-for="Bio"></label>
                                        <textarea asp-for="Bio" class="form-control" rows="4"
                                                  placeholder="Enter author biography"></textarea>
                                        <span asp-validation-for="Bio" class="text-danger"></span>
                                    </div>

                                    <div class="form-group">
                                        <label asp-for="CountryCode"></label>
                                        <select asp-for="CountryCode" class="form-control"
                                                asp-items="Html.GetEnumSelectList<AseerAlkotb.Domain.Enums.CountryCode>()"></select>
                                        <span asp-validation-for="CountryCode" class="text-danger"></span>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- RIGHT: Author Image -->
                        <div class="col-md-4">
                            <div class="card">
                                <div class="card-header">
                                    <h3 class="card-title">Author Image</h3>
                                </div>
                                <div class="card-body">
                                    <div class="form-group">
                                        <div class="text-center mb-3">
                                            <img id="imagePreview" src="@Model.Image != null ? " /images/" + Model.Image.FileName : "/images/default-user.png" " class="img-circle"
                                                 style="width: 150px; height: 150px; object-fit: cover;" />
                                        </div>
                                        <div class="custom-file">
                                            <input type="file" asp-for="Image" class="custom-file-input" id="imageInput"
                                                   accept="image/*" />
                                            <label class="custom-file-label" for="imageInput">Choose image...</label>
                                        </div>
                                        <small class="form-text text-muted">Supported formats: JPG, PNG. Max size: 2MB</small>
                                        <span asp-validation-for="Image" class="text-danger"></span>
                                    </div>
                                </div>
                            </div>

                            <div class="card mt-4">
                                <div class="card-header">
                                    <h3 class="card-title">Preview</h3>
                                </div>
                                <div class="card-body">
                                    <div class="text-center mb-3">
                                        <h4 id="previewName" class="text-muted">@Model.Name</h4>
                                        <p id="previewCountry" class="text-muted">@Model.CountryCode</p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="row mt-4">
                        <div class="col-md-12 text-right">
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-save"></i> Save Changes
                            </button>
                            <a asp-action="Index" class="btn btn-secondary ml-2">
                                <i class="fas fa-times"></i> Cancel
                            </a>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    <script>
        $(function () {
            // Image preview
            $(document).on("change", "#imageInput", function() {
                var reader = new FileReader();
                reader.onload = function(e) {
                    $("#imagePreview").attr("src", e.target.result);
                }
                reader.readAsDataURL(this.files[0]);
            });

            // Update preview
            $(document).on("input", "#Name", function() {
                $("#previewName").text($(this).val() || "Author Name");
            });

            $(document).on("change", "#CountryCode", function() {
                var countryCode = $(this).find("option:selected").text();
                $("#previewCountry").text(countryCode || "Country");
            });

            // Form validation
            $("#editAuthorForm").on("submit", function(e) {
                if (!$('#Name').val()) {
                    e.preventDefault();
                    alert("Please enter author name");
                }
            });
        });
    </script>
}